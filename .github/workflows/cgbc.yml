# Simple workflow for deploying static content to GitHub Pages
name: Deploy CGCA Web

on:
  # Runs on pushes targeting the default branch
  push:
    branches: ["master"]
    paths: [ web/cgca/** ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# Allow only one concurrent deployment, skipping runs queued between the run in-progress and latest queued.
# However, do NOT cancel in-progress runs as we want to allow these production deployments to complete.
concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  # Single deploy job since we're just deploying
  deploy:
    runs-on: [ self-hosted ]
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          fetch-depth: 2
      - name: Get Changed Files
        id: changed-files
        uses: tj-actions/changed-files@v32
      - name: Generate Build Number
        id: buildid
        uses: josStorer/get-current-time@v2.0.2
        with:
          utcOffset: "00:00"
          format: YYYY.MM.DD-HH.mm
      - name: List Modified Files
        run: |
          Set-Content -Path C:/deployment-stage/cgca-changes.txt -Value "Commit Item Messages:"
          Add-Content -Path C:/deployment-stage/cgca-changes.txt -Value "---------------------------------"
          Add-Content -Path C:/deployment-stage/cgca-changes.txt -Value $(git log --format=%B -n 1 HEAD)
          Add-Content -Path C:/deployment-stage/cgca-changes.txt -Value ""
          Add-Content -Path C:/deployment-stage/cgca-changes.txt -Value "---------------------------------"
          Add-Content -Path C:/deployment-stage/cgca-changes.txt -Value "Build Number - ${{ steps.buildid.outputs.formattedTime }}"
          Add-Content -Path C:/deployment-stage/cgca-changes.txt -Value "---------------------------------"
          Add-Content -Path C:/deployment-stage/cgca-changes.txt -Value "Changed Files"
          foreach ($file in "${{ steps.changed-files.outputs.all_modified_files }}") {
            Add-Content -Path C:/deployment-stage/cgca-changes.txt -Value "\r\n$file"
          }
      - name: Create Backup
        run: |
          New-Item -ItemType "directory" -Path "C:/deployment-stage/cgca/${{ steps.buildid.outputs.formattedTime }}/" -Force
          Copy-Item C:/inetpub/cedargrovechristianacademy.org/* -Destination C:/deployment-stage/cgca/${{ steps.buildid.outputs.formattedTime }}/ -Recurse
          Compress-Archive -Path "C:/deployment-stage/cgca/${{ steps.buildid.outputs.formattedTime }}/*" -Update -DestinationPath C:/backups/cgca-${{ steps.buildid.outputs.formattedTime }}.zip -CompressionLevel "Fastest"
          
